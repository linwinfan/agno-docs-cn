---
title: 异步 MongoDB
sidebarTitle: 概述
description: 学习异步使用 MongoDB 作为您的代理数据库
---

<Badge icon="code-branch" color="orange">
    <Tooltip tip="Introduced in v2.2.3" cta="View release notes" href="https://github.com/agno-agi/agno/releases/tag/v2.2.3">v2.2.3</Tooltip>
</Badge>

Agno 支持异步使用 [MongoDB](https://www.mongodb.com/)，通过 `AsyncMongoDb` 类实现。

<Tip>
  **v2 迁移支持**：如果您正在从 Agno v1 升级，MongoDB 在 v2 迁移脚本中完全受支持。请参阅[迁移指南](/how-to/v2-migration)了解详细信息。
</Tip>

## 用法

```python async_mongodb_for_agent.py
from agno.agent import Agent
from agno.db.mongo import AsyncMongoDb

# MongoDB 连接设置
db_url = "mongodb://localhost:27017"

db = AsyncMongoDb(db_url=db_url)

# 使用数据库设置您的代理
agent = Agent(db=db)
```

### 运行 MongoDB

安装 [docker desktop](https://docs.docker.com/desktop/install/mac-install/) 并使用以下命令在端口 **27017** 上运行 **MongoDB**：
```bash
docker run -d \
  --name local-mongo \
  -p 27017:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=mongoadmin \
  -e MONGO_INITDB_ROOT_PASSWORD=secret \
  mongo
```


## 参数

<Snippet file="db-async-mongo-params.mdx" />

## 开发者资源

- 查看[示例代码](https://github.com/agno-agi/agno/tree/main/cookbook/07_database/mongo/async_mongodb_for_agent.py)
