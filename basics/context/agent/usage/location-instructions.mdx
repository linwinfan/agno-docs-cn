---
title: 位置感知的智能体指令
---

本示例演示了如何向智能体指令中添加位置上下文，使智能体能够提供特定位置的响应并搜索本地新闻。

## 代码

```python location_instructions.py
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.duckduckgo import DuckDuckGoTools

agent = Agent(
    model=OpenAIChat(id="gpt-5-mini"),
    add_location_to_context=True,
    tools=[DuckDuckGoTools(cache_results=True)],
)
agent.print_response("我在哪个城市？")
agent.print_response("关于我所在城市的最新新闻是什么？")
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="安装库">
    ```bash
    pip install -U agno openai ddgs
    ```
  </Step>

  <Step title="导出您的 OpenAI API 密钥">

    <CodeGroup>

    ```bash Mac/Linux
      export OPENAI_API_KEY="your_openai_api_key_here"
    ```

    ```bash Windows
      $Env:OPENAI_API_KEY="your_openai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="创建 Python 文件">
    创建一个 Python 文件并添加上述代码。
    ```bash
    touch location_instructions.py
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python location_instructions.py
    ```

    ```bash Windows
    python location_instructions.py
    ```
    </CodeGroup>
  </Step>

  <Step title="查找所有 Cookbook">
    探索 Agno 仓库中所有可用的 cookbook。点击下面的链接在 GitHub 上查看代码：

    <Link href="https://github.com/agno-agi/agno/tree/main/cookbook/03_agents/context_management" target="_blank">
      Agno Cookbook on GitHub
    </Link>
  </Step>
</Steps>