---
title: 上下文工程
sidebarTitle: 概述
description: 了解如何为您的团队编写提示和其他上下文工程技术。
keywords: [上下文, 上下文工程, 系统消息, 用户消息, 提示, 提示工程]
---

上下文工程是设计和控制发送给语言模型的信息（上下文）以指导其行为和输出的过程。
在实践中，构建上下文归结为一个问题："哪些信息最有可能实现期望的结果？"

有效的上下文工程是一个迭代过程：优化系统消息，尝试不同的描述和指令，并使用模式、委派和工具集成等功能。

Agno 团队的上下文包括以下内容：
- **系统消息**：系统消息是发送给团队的主要上下文，包括所有附加上下文
- **用户消息**：用户消息是发送给团队的消息。
- **聊天历史**：聊天历史是团队与用户之间对话的历史记录。
- **附加输入**：任何少量示例或其他附加输入，这些都会添加到上下文中。

## 系统消息上下文

以下是用于创建系统消息的一些关键参数：

1. **描述**：指导团队整体行为的描述。
2. **指令**：关于如何实现其目标的精确、任务特定指令列表。
3. **预期输出**：对团队预期输出的描述。
4. **成员**：有关团队成员、其角色和能力的信息。

系统消息由团队的描述、指令、成员详细信息和其他设置构建而成。团队领导者的系统消息还额外包含委派规则和协调指南。例如：

```python instructions.py
from agno.agent import Agent
from agno.team import Team
from agno.tools.duckduckgo import DuckDuckGoTools
from agno.tools.hackernews import HackerNewsTools

web_agent = Agent(
    name="Web Researcher",
    role="您是一位网络研究员，可以在网上查找信息。",
    instructions=[
        "使用您的网络搜索工具在网上查找信息。",
        "提供所找到信息的摘要。",
    ],
    tools=[DuckDuckGoTools()],
    markdown=True,
    debug_mode=True,  # 设置为 True 以查看详细日志
)
hackernews_agent = Agent(
    name="HackerNews Researcher",
    role="您是一位 HackerNews 研究员，可以在 HackerNews 上查找信息。",
    instructions=[
        "使用您的 HackerNews 搜索工具在 HackerNews 上查找信息。",
        "提供所找到信息的摘要。",
    ],
    tools=[HackerNewsTools()],
    markdown=True,
    debug_mode=True,  # 设置为 True 以查看详细日志
)

team = Team(
    members=[web_agent, hackernews_agent],
    instructions=[
        "您是一个研究团队，可以在网络和 HackerNews 上查找信息。",
        "找到有关主题的信息后，编写一份联合报告。"
    ],
    markdown=True,
    debug_mode=True,   # 设置为 True 以查看详细日志并查看编译后的系统消息
)
team.print_response("加密货币市场的最新消息是什么？", stream=True)
```

将产生以下系统消息：

```
您是 AI 智能体团队和子团队的领导者。
您的任务是协调团队完成用户的请求。

以下是您团队中的成员：
<team_members>
- Agent 1:
    - ID: web-researcher
    - Name: Web Researcher
    - Role: 您是一位网络研究员，可以在网上查找信息。
    - Member tools:
        - duckduckgo_search
        - duckduckgo_news
- Agent 2:
    - ID: hacker-news-researcher
    - Name: HackerNews Researcher
    - Role: 您是一位 HackerNews 研究员，可以在 HackerNews 上查找信息。
    - Member tools:
        - get_top_hackernews_stories
        - get_user_details
</team_members>

<how_to_respond>
- 您的角色是将任务转发给您团队中最有能力完成用户请求的成员。
- 在委派任务之前，请仔细分析成员可用的工具及其角色。
- 您不能直接使用成员工具。您只能将任务委派给成员。
- 当您将任务委派给其他成员时，请确保包含：
    - member_id (str)：要委派任务的成员 ID。仅使用成员的 ID，不要使用团队 ID 后跟成员 ID。
    - task_description (str)：任务的清晰描述。
    - expected_output (str)：预期输出。
- 您可以同时将任务委派给多个成员。
- 在响应用户之前，您必须始终分析成员的响应。
- 在分析成员的响应后，如果您认为任务已完成，可以停止并响应用户。
- 如果您对成员的响应不满意，应该重新分配任务。
- 对于简单的问候、感谢或关于团队本身的问题，您应该直接响应。
- 对于所有工作请求、任务或需要专业知识的问题，请路由到适当的团队成员。
</how_to_respond>

<instructions>
- 您是一个研究团队，可以在网络和 HackerNews 上查找信息。
- 找到有关主题的信息后，编写一份联合报告。
</instructions>

<additional_information>
- 使用 markdown 格式化您的答案。
</additional_information>
```

### 系统消息参数

团队创建一个默认的系统消息，可以使用以下参数进行自定义：

| 参数 | 类型 | 默认值 | 描述 |
| --- | --- | --- | --- |
| `description` | `str` | `None` | 添加到系统消息开头的团队描述。 |
| `instructions` | `List[str]` | `None` | 添加到系统提示中的指令列表，用 `<instructions>` 标签包裹。根据 `markdown`、`expected_output` 等值也会创建默认指令。 |
| `additional_context` | `str` | `None` | 添加到系统消息末尾的附加上下文。 |
| `expected_output` | `str` | `None` | 提供团队的预期输出。这会添加到系统消息的末尾。 |
| `markdown` | `bool` | `False` | 添加一条指令，要求使用 markdown 格式化输出。 |
| `add_datetime_to_context` | `bool` | `False` | 如果为 True，将当前日期时间添加到提示中，使团队具有时间感。这允许在提示中使用"明天"等相对时间 |
| `add_name_to_context` | `bool` | `False` | 如果为 True，将团队的名称添加到上下文中。 |
| `add_location_to_context` | `bool` | `False` | 如果为 True，将团队的位置添加到上下文中。这允许位置感知的响应和本地上下文。 |
| `timezone_identifier` | `str` | `None` | 允许为日期时间指令使用自定义时区，遵循 TZ 数据库格式（例如"Etc/UTC"） |
| `add_member_tools_to_context` | `bool` | `True` | 如果为 True，将团队成员可用的工具添加到上下文中。 |
| `add_session_summary_to_context` | `bool` | `False` | 如果为 True，将会话摘要添加到上下文中。有关更多信息，请参阅[会话](/basics/sessions/overview)。 |
| `add_memories_to_context` | `bool` | `False` | 如果为 True，将用户记忆添加到上下文中。有关更多信息，请参阅[记忆](/basics/memory/overview)。 |
| `add_dependencies_to_context` | `bool` | `False` | 如果为 True，将依赖项添加到上下文中。有关更多信息，请参阅[依赖项](/basics/dependencies/overview)。 |
| `add_session_state_to_context` | `bool` | `False` | 如果为 True，将会话状态添加到上下文中。有关更多信息，请参阅[状态](/basics/state/overview)。 |
| `add_knowledge_to_context` | `bool` | `False` | 如果为 True，将检索到的知识添加到上下文中，以启用 RAG。有关更多信息，请参阅[知识](/basics/knowledge/overview)。 |
| `enable_agentic_knowledge_filters` | `bool` | `False` | 如果为 True，让团队选择知识过滤器。有关更多信息，请参阅[知识](/basics/knowledge/filters/overview)。 |
| `system_message` | `str` | `None` | 覆盖默认的系统消息。 |
| `respond_directly` | `bool` | `False` | 如果为 True，团队领导者不会处理成员的响应，而是直接返回它们。 |
| `delegate_to_all_members` | `bool` | `False` | 如果为 True，团队领导者将同时将任务委派给所有成员，而不是逐个委派。当异步运行（使用 `arun`）时，成员将并发运行。 |
| `determine_input_for_members` | `bool` | `True` | 如果您希望直接将运行输入发送给成员智能体，请设置为 false。 |
| `share_member_interactions` | `bool` | `False` | 如果为 True，将所有之前的成员交互发送给成员。 |
| `get_member_information_tool` | `bool` | `False` | 如果为 True，添加一个工具来获取团队成员的信息。 |

有关更多信息，请参阅完整的[团队参考](/reference/teams/team)。

### 系统消息的构建方式

让我们看以下示例团队：

```python
from agno.agent import Agent
from agno.team import Team

web_agent = Agent(
    name="Web Researcher",
    role="您是一位网络研究员，可以在网上查找信息。",
    description="您是一个乐于助人的网络研究助手",
    instructions=["搜索准确的信息"],
    markdown=True,
)

team = Team(
    members=[web_agent],
    name="Research Team",
    role="Team Lead",
    description="您是研究团队负责人",
    instructions=["协调团队提供全面的研究"],
    expected_output="您应该使用详细的发现格式化您的响应",
    markdown=True,
    add_datetime_to_context=True,
    add_location_to_context=True,
    add_name_to_context=True,
    add_session_summary_to_context=True,
    add_memories_to_context=True,
    add_session_state_to_context=True,
)
```

以下是将构建的系统消息：
```
您是 AI 智能体团队和子团队的领导者。
您的任务是协调团队完成用户的请求。

以下是您团队中的成员：
<team_members>
- Agent 1:
    - ID: web-researcher
    - Name: Web Researcher
    - Role: 您是一位网络研究员，可以在网上查找信息。
    - Member tools:
        (none)
</team_members>

<how_to_respond>
...
</how_to_respond>

您可以访问之前与用户的交互记忆，可以使用：

<memories_from_previous_interactions>
- 用户非常喜欢数码宝贝和日本。
- 用户非常喜欢日本。
- 用户喜欢咖啡。
</memories_from_previous_interactions>

注意：此信息来自之前的交互，可能会在本次对话中更新。您应该始终优先考虑本次对话中的信息，而不是过去的记忆。

以下是您之前交互的简要摘要：

<summary_of_previous_interactions>
用户询问了有关数码宝贝和日本的信息。
</summary_of_previous_interactions>

注意：此信息来自之前的交互，可能已过时。您应该始终优先考虑本次对话中的信息，而不是过去的摘要。

<description>
您是研究团队负责人
</description>

<your_role>
Team Lead
</your_role>

<instructions>
- 协调团队提供全面的研究
</instructions>

<additional_information>
- 使用 markdown 格式化您的答案。
- 当前时间是 2025-09-30 12:00:00。
- 您的大致位置是：纽约，纽约州，美国。
- 您的名字是：Research Team。
</additional_information>

<expected_output>
您应该使用详细的发现格式化您的响应
</expected_output>

<session_state> ... </session_state>
```

<Tip>
此示例是详尽的，说明了系统消息的可能性，但在实践中，您只会使用其中一些设置。
</Tip>

#### 附加上下文

您可以使用 `additional_context` 参数将附加上下文添加到系统消息的末尾。

在这里，`additional_context` 在系统消息中添加了一个注释，表明团队可以访问特定的数据库表。

```python
from textwrap import dedent

from agno.agent import Agent
from agno.team import Team
from agno.models.langdb import LangDB
from agno.tools.duckdb import DuckDbTools

duckdb_tools = DuckDbTools(
    create_tables=False, export_tables=False, summarize_tables=False
)
duckdb_tools.create_table_from_path(
    path="https://phidata-public.s3.amazonaws.com/demo_data/IMDB-Movie-Data.csv",
    table="movies",
)

web_researcher = Agent(
    name="Web Researcher",
    role="您是一位网络研究员，可以在网上查找信息。",
    tools=[DuckDbTools()],
    instructions=[
        "使用您的网络搜索工具在网上查找信息。",
        "提供所找到信息的摘要。",
    ],
)

team = Team(
    members=[web_researcher],
    model=LangDB(id="llama3-1-70b-instruct-v1.0"),
    tools=[duckdb_tools],
    markdown=True,
    additional_context=dedent("""\
    您可以访问以下表格：
    - movies：包含来自 IMDB 的电影信息。
    """),
)
team.print_response("电影的平均评分是多少？", stream=True)
```

#### 团队成员信息

成员信息会自动注入到系统消息中。这包括成员 ID、名称、角色和工具。
您可以通过将 `add_member_tools_to_context` 设置为 False 来选择性地最小化此信息，这会从系统消息中移除成员工具。

您还可以给团队领导者一个工具来获取团队成员的信息。

```python
from agno.agent import Agent
from agno.team import Team

web_agent = Agent(
    name="Web Researcher",
    role="您是一位网络研究员，可以在网上查找信息。"
)

team = Team(
    members=[web_agent],
    get_member_information_tool=True,  # 添加一个工具来获取团队成员的信息
)
```


#### 工具指令

如果您在团队上使用了[工具包](/integrations/toolkits/overview)，可以使用 `instructions` 参数将工具指令添加到系统消息中：

```python
from agno.agent import Agent
from agno.tools.slack import SlackTools

slack_tools = SlackTools(
    instructions=["使用 `send_message` 向用户发送消息。如果用户指定了线程，请使用 `send_message_thread` 向线程发送消息。"],
    add_instructions=True,
)
team = Team(
    members=[...],
    tools=[slack_tools],
)
```

这些指令会在 `<additional_information>` 标签后注入到系统消息中。

#### 智能体记忆

如果您的团队上设置了 `enable_agentic_memory` 为 `True`，团队将能够使用工具创建/更新用户记忆。

这会向系统消息添加以下内容：
```
<updating_user_memories>
- 您可以访问 `update_user_memory` 工具，用于添加新记忆、更新现有记忆、删除记忆或清除所有记忆。
- 如果用户消息包含应作为记忆捕获的信息，请使用 `update_user_memory` 工具更新您的记忆数据库。
- 记忆应包含可以个性化与用户持续交互的详细信息。
- 使用此工具添加新记忆或更新您在对话中识别的现有记忆。
- 如果用户要求更新他们的记忆、删除记忆或清除所有记忆，请使用此工具。
- 如果您使用 `update_user_memory` 工具，请记住将响应传递给用户。
</updating_user_memories>
```

#### 智能体知识过滤器

如果您的团队启用了知识功能，可以使用 `enable_agentic_knowledge_filters` 参数让团队选择知识过滤器。

这将向系统消息添加以下内容：
```
知识库包含具有这些元数据过滤器的文档：[filter1, filter2, filter3]。
当用户查询指示特定元数据时，始终使用过滤器。

示例：
1. 如果用户询问特定人员（如"Jordan Mitchell"），您必须使用 search_knowledge_base 工具，并将 filters 参数设置为 {{'<valid key like user_id>': '<valid value based on the user query>'}}。
2. 如果用户询问特定文档类型（如"contracts"），您必须使用 search_knowledge_base 工具，并将 filters 参数设置为 {{'document_type': 'contract'}}。
4. 如果用户询问特定位置（如"来自纽约的文档"），您必须使用 search_knowledge_base 工具，并将 filters 参数设置为 {{'<valid key like location>': 'New York'}}。

一般指南：
- 始终分析用户查询以识别相关元数据。
- 使用尽可能具体的过滤器来缩小结果范围。
- 如果多个过滤器相关，请在 filters 参数中组合它们（例如，{{'name': 'Jordan Mitchell', 'document_type': 'contract'}}）。
- 确保过滤器键与有效的元数据过滤器匹配：[filter1, filter2, filter3]。

您可以使用 search_knowledge_base 工具搜索知识库并获取最相关的文档。确保将过滤器作为 [Dict[str: Any]] 传递给工具。严格遵循此结构。
```

在[知识过滤器](/basics/knowledge/filters/overview)部分中详细了解智能体知识过滤器。

### 直接设置系统消息

您可以使用 `system_message` 参数手动设置系统消息。这将忽略所有其他设置，使用您提供的系统消息。

```python
from agno.team import Team

team = Team(members=[], system_message="分享一个关于")
team.print_response("公元12000年的爱情的两句话故事。")
```

## 用户消息上下文

发送给 `Team.run()` 或 `Team.print_response()` 的 `input` 用作用户消息。

请参阅[依赖项](/basics/dependencies/overview)了解如何为用户消息进行依赖注入。

### 附加用户消息上下文

默认情况下，用户消息是使用发送给 `Team.run()` 或 `Team.print_response()` 函数的 `input` 构建的。

以下团队参数配置用户消息的构建方式：
- `add_knowledge_to_context`
- `add_dependencies_to_context`

```python
from agno.agent import Agent
from agno.team import Team

web_agent = Agent(
    name="Web Researcher",
    role="您是一位网络研究员，可以在网上查找信息。"
)

team = Team(
    members=[web_agent],
    add_knowledge_to_context=True,
    add_dependencies_to_context=True
)
team.print_response("法国的首都是什么？", dependencies={"name": "John Doe"})
```

发送给模型的用户消息将如下所示：
```
法国的首都是什么？

如果有助于回答，请使用知识库中的以下参考：
<references>
- Reference 1
- Reference 2
</references>

<additional context>
{"name": "John Doe"}
</additional context>
```

## 聊天历史

如果您的团队启用了数据库存储，会话历史会自动存储（参见[会话](/basics/sessions/overview)）。

现在您可以使用 `add_history_to_context` 将对话历史添加到上下文中。

```python
from agno.agent import Agent
from agno.team import Team
from agno.tools.duckduckgo import DuckDuckGoTools
from agno.db.postgres import PostgresDb
from agno.models.openai import OpenAIChat

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

db = PostgresDb(db_url=db_url)

web_researcher = Agent(
    name="Web Researcher",
    role="您是一位网络研究员，可以在网上查找信息。",
    tools=[DuckDuckGoTools()],
    instructions=[
        "使用您的网络搜索工具在网上查找信息。",
        "提供所找到信息的摘要。",
    ],
)

team = Team(
    members=[web_researcher],
    model=OpenAIChat(id="gpt-5-mini"),
    db=db,
    session_id="chat_history",
    instructions="您是一个乐于助人的助手，可以回答有关太空和海洋的问题。",
    add_history_to_context=True,
    num_history_runs=2,  # 可选地限制要添加到上下文中的历史响应数量
)

team.print_response("宁静海在哪里？")

team.print_response("我的第一个问题是什么？")
```

这将把对话历史添加到上下文中，可用于为下一条消息提供上下文。

有关更多详细信息，请参阅[会话](/basics/chat-history/overview)。

<Note>所有团队成员的运行都会添加到团队会话历史中。</Note>

## 管理工具调用

<Badge icon="code-branch" color="orange">
    <Tooltip tip="在 v2.2.1 中引入" cta="查看发布说明" href="https://github.com/agno-agi/agno/releases/tag/v2.2.1">v2.2.1</Tooltip>
</Badge>

`max_tool_calls_from_history` 参数可用于仅将历史中最新的 `n` 个工具调用添加到上下文中。

这有助于管理上下文大小并在团队运行期间减少令牌成本。

```python
from agno.agent import Agent
from agno.db.sqlite import SqliteDb
from agno.models.openai import OpenAIChat
from agno.team import Team
from agno.tools.duckduckgo import DuckDuckGoTools

web_agent = Agent(
    name="Web Researcher",
    model=OpenAIChat(id="gpt-4o-mini"),
    instructions="您是一位网络研究员。在网上搜索有关给定主题的全面信息。",
    tools=[DuckDuckGoTools()],
)

team = Team(
    members=[web_agent],
    model=OpenAIChat(id="gpt-4o"),
    db=SqliteDb(db_file="tmp/filter_history_tool_calls_team.db"),
    add_history_to_context=True,
    max_tool_calls_from_history=5,  # 仅在上下文中保留最近5个工具调用
    show_members_responses=True,
)

team.print_response("搜索 AI 新闻")
team.print_response("搜索加密货币趋势")
team.print_response("搜索科技股")
# 较早的搜索将从上下文中过滤掉
team.print_response("搜索初创公司")
team.print_response("搜索孟买的天气")
team.print_response("搜索最新的初创公司融资")
team.print_response("我最近搜索了哪些主题？")
```
在此示例中：
- **运行 1-3：** 团队看到工具调用 [1], [1,2], [1,2,3,4,5]
- **运行 4：** 团队看到适合最近5个工具调用的运行中的工具调用（较早的工具调用被过滤掉）
- **运行 5：** 团队看到适合最近5个工具调用的运行中的工具调用（更早的工具调用被过滤掉）

<Note>
**重要：** `max_tool_calls_from_history` 从 `num_history_runs` 加载的运行中过滤工具调用。您的数据库始终包含完整的历史记录。
</Note>

请参阅[完整示例](/basics/context/team/usage/filter-tool-calls-from-history)以获得完整的演示。

## 附加输入

您可以使用 `additional_input` 参数向团队的上下文中添加完整的附加消息。
这些消息会被添加到上下文中，就像它们是对话历史的一部分一样。

您可以给您的团队提供它应该如何响应的示例（也称为"少量提示"）：

```python
from agno.team import Team
from agno.models.message import Message
from agno.models.openai.chat import OpenAIChat

# 少量示例展示良好的支持模式
support_examples = [
    # 示例 1：简单问题解决
    Message(role="user", content="我忘记了密码，无法登录"),
    Message(
        role="assistant",
        content="""
    我会立即帮您重置密码。

    **重置密码的步骤：**
    . 转到登录页面并点击"忘记密码"
    . 输入您的电子邮件地址
    . 检查您的电子邮件中的重置链接
    . 点击链接创建新密码
    . 使用包含大写、小写、数字和符号的强密码

    如果5分钟内没有收到邮件，请检查您的垃圾邮件文件夹。如果您需要这些步骤的任何帮助，请告诉我！
    """.strip(),
    ),
    # 示例 2：账单问题模式
    Message(
        role="user",
        content="我被重复收取了同一订单的费用，我很沮丧！",
    ),
    Message(
        role="assistant",
        content="""
    对于账单错误以及由此给您带来的沮丧，我深表歉意。

    **立即行动计划：**
    1. 我将调查您的账户以确认重复收费
    2. 处理重复交易的全额退款
    3. 通过检查您的支付方式设置确保这种情况不再发生
    4. 退款处理完成后为您提供确认号码

    退款通常需要3-5个工作日才能显示在您的账单上。作为对此不便的道歉，我还想为您提供下次订单10%的折扣。

    关于此问题，还有其他我可以帮您的吗？
    """.strip(),
    ),
    # 示例 3：技术支持模式
    Message(role="user", content="当我尝试上传照片时，您的应用程序不断崩溃"),
    Message(
        role="assistant",
        content="""
    很抱歉您在上传照片时遇到崩溃问题。让我帮您解决这个问题。

    **故障排除步骤：**
    1. **检查应用程序版本：** 确保您从应用商店获得了最新版本
    2. **重新启动应用程序：** 完全关闭并重新打开应用程序
    3. **检查存储空间：** 确保您的设备有足够的存储空间（至少1GB可用）
    4. **照片大小：** 尝试上传较小的照片（每张小于10MB）
    5. **网络连接：** 确保您有稳定的互联网连接

    **如果问题仍然存在：**
    - 尝试一次只上传一张照片，而不是多张
    - 在设备设置中清除应用程序缓存
    - 重新启动您的设备

    如果这些步骤都无法解决问题，请告诉我您的设备类型和操作系统版本，我会将此问题升级给我们的技术团队进行进一步调查。
    """.strip(),
    ),
]

if __name__ == "__main__":
    # 创建带有少量学习的团队
    team = Team(
        members=[...],
        name="Customer Support Team",
        model=OpenAIChat(id="gpt-5-mini"),
        add_name_to_context=True,
        additional_input=support_examples,  # 少量学习示例
        instructions=[
            "您是一位专业的客户支持专家。",
            "始终保持同理心、专业性和以解决方案为导向。",
            "提供清晰、可操作的步骤来解决客户问题。",
            "遵循既定模式，提供一致、高质量的支持。",
        ],
        markdown=True,
    )

    for i, example in enumerate(support_examples, 1):
        print(f"📞 示例 {i}: {example}")
        print("-" * 50)
        team.print_response(example)
```

## 上下文缓存

大多数模型提供商都支持系统和用户消息的缓存，尽管不同提供商的实现方式有所不同。

通用方法是缓存重复内容和常见指令，然后在后续请求中将这些缓存内容作为系统消息的前缀重用。换句话说，如果模型支持缓存，您可以通过将静态内容放在系统消息的开头来减少发送的令牌数量。

Agno 的上下文构建设计将最可能的静态内容放在系统消息的开头。如果您想要更多控制，可以通过手动设置系统消息来微调这一点。

对于团队，成员信息、委派指令和协调指南通常是静态的，因此是缓存的强有力候选者。

一些提示缓存的示例：
- [OpenAI 的提示缓存](https://platform.openai.com/docs/guides/prompt-caching)
- [Anthropic 提示缓存](https://docs.claude.com/en/docs/build-with-claude/prompt-caching) -> 查看 [Agno 示例](/integrations/models/native/anthropic/usage/prompt-caching)
- [OpenRouter 提示缓存](https://openrouter.ai/docs/features/prompt-caching)

## 开发者资源

- 查看[团队模式](/reference/teams/team)
- 查看[Cookbook](https://github.com/agno-agi/agno/tree/main/cookbook/04_teams)