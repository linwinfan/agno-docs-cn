---
title: Pandas 工具
description: 使用 agno 库和 PandasTools 实现的 AI 智能体，用于自动化数据分析。
---

## 代码

```python cookbook/14_tools/pandas_tools.py
from agno.agent import Agent
from agno.tools.pandas import PandasTools

agent = Agent(
    tools=[PandasTools()],
        markdown=True,
)
agent.print_response("从 data.csv 加载并分析数据集")
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置你的 API 密钥">
    ```bash
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U pandas openai agno
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python cookbook/14_tools/pandas_tools.py
    ```

    ```bash Windows
    python cookbook/14_tools/pandas_tools.py
    ```
    </CodeGroup>
  </Step>
</Steps>
