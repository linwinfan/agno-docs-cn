---
title: Redshift 工具
sidebarTitle: Redshift 工具
keywords: [redshift, aws, 数据库, sql, 数据仓库]
description: 本示例演示如何使用 RedshiftTools 使智能体能够与 Amazon Redshift 数据仓库交互，支持标准和 IAM 身份验证。
mode: wide
---


<Steps>

  <Step title="创建一个 Python 文件">
    创建一个 Python 文件并添加以下代码。
    ```bash
    touch redshift_tools.py
    ```
  </Step>

  <Step title="将以下代码添加到你的 Python 文件中">
    ```python redshift_tools.py
    from agno.agent import Agent
    from agno.models.openai import OpenAIChat
    from agno.tools.redshift import RedshiftTools

    agent = Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[
            RedshiftTools(
                iam=True,
                table_schema="public",
            )
        ],
        name="Redshift 智能体",
        description="一个可以在 Amazon Redshift 中查询和分析数据的智能体。",
        instructions=[
            "你是可以访问 Redshift 数据仓库的数据分析师助手。",
            "使用可用的工具列出表、描述模式并运行 SQL 查询。",
            "在运行查询之前始终检查它们以确保已优化。",
            "提供清晰的发现解释。",
        ],
        markdown=True,
    )

    # 列出表并描述其中一个
    response = agent.run(
        "列出数据库中的所有表并描述销售表的模式。"
    )
    print(f"智能体响应:\n{response.content}\n")

    # 运行查询
    response = agent.run(
        "编写并运行一个查询以获取总收入排名前 10 的客户。"
    )
    print(f"智能体响应:\n{response.content}")
    ```
  </Step>

  <Snippet file="create-venv-step.mdx" />

  <Step title="安装库">
    ```bash
    pip install -U agno openai redshift-connector
    ```
  </Step>

  <Step title="导出你的 OpenAI API 密钥">
    <CodeGroup>
    ```bash Mac/Linux
      export OPENAI_API_KEY="your_openai_api_key_here"
    ```

    ```bash Windows
      $Env:OPENAI_API_KEY="your_openai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="设置你的 Redshift 连接详情">
    设置你的 AWS 凭据和 Redshift 连接详情：
    <CodeGroup>
    ```bash Mac/Linux
      export REDSHIFT_HOST="your-cluster.abc123.us-east-1.redshift.amazonaws.com"
      export REDSHIFT_DATABASE="dev"
      export AWS_ACCESS_KEY_ID="your-access-key"
      export AWS_SECRET_ACCESS_KEY="your-secret-key"
      export AWS_SESSION_TOKEN="your-session-token"
    ```

    ```bash Windows
      $Env:REDSHIFT_HOST="your-cluster.abc123.us-east-1.redshift.amazonaws.com"
      $Env:REDSHIFT_DATABASE="dev"
      $Env:AWS_ACCESS_KEY_ID="your-access-key"
      $Env:AWS_SECRET_ACCESS_KEY="your-secret-key"
      $Env:AWS_SESSION_TOKEN="your-session-token"  # if using temporary credentials
    ```
    </CodeGroup>
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python redshift_tools.py
    ```

    ```bash Windows
    python redshift_tools.py
    ```
    </CodeGroup>
  </Step>

  <Step title="查找所有示例代码">
  浏览 Agno 仓库中所有可用的示例代码。点击下面的链接在 GitHub 上查看代码：

  <Link href="https://github.com/agno-agi/agno/tree/main/cookbook/14_tools" target="_blank">
    Agno Tools Cookbooks on GitHub
  </Link>
</Step>
</Steps>
