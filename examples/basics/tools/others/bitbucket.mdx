---
title: Bitbucket 工具
---


## 代码

```python cookbook/14_tools/bitbucket_tools.py
from agno.agent import Agent
from agno.tools.bitbucket import BitbucketTools

agent = Agent(
    instructions=[
        "使用您的工具回答关于Bitbucket仓库的问题",
        "除非明确要求，否则不要创建任何问题或拉取请求",
    ],
    tools=[BitbucketTools(
        workspace="your-workspace",
        repo_slug="your-repository"
    )],
    markdown=True,
)

agent.print_response("List all open pull requests in the repository")
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的Bitbucket凭证">
    ```bash
    export BITBUCKET_USERNAME=your-username
    export BITBUCKET_PASSWORD=your-app-password
    export OPENAI_API_KEY=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U requests openai agno
    ```
  </Step>

  <Step title="运行代理">
    <CodeGroup>
    ```bash Mac
    python cookbook/14_tools/bitbucket_tools.py
    ```

    ```bash Windows
    python cookbook/14_tools/bitbucket_tools.py
    ```
    </CodeGroup>
  </Step>
</Steps>
