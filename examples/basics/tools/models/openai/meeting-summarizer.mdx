---
title: 会议摘要智能体
description: 多模态 Agno 智能体，使用 OpenAI 工具转录会议录音、提取关键见解、生成可视化摘要并创建音频摘要。
---


此示例演示一个多模态会议摘要和可视化智能体，该智能体使用 OpenAITools 和 ReasoningTools 来转录会议录音、提取关键见解、生成可视化摘要并合成音频摘要。

## 代码

```python ref/meeting_summarizer_agent.py
"""示例：会议摘要与可视化智能体

此脚本使用 OpenAITools (transcribe_audio, generate_image, generate_speech)
来处理会议录音，对其进行摘要，可视化，并创建音频摘要。

需要：pip install openai agno
"""

import base64
from pathlib import Path
from textwrap import dedent

from agno.agent import Agent
from agno.models.google import Gemini
from agno.tools.openai import OpenAITools
from agno.tools.reasoning import ReasoningTools
from agno.utils.media import download_file, save_base64_data

input_audio_url: str = (
    "https://agno-public.s3.us-east-1.amazonaws.com/demo_data/sample_audio.mp3"
)

local_audio_path = Path("tmp/meeting_recording.mp3")
print(f"正在下载文件到本地路径: {local_audio_path}")
download_file(input_audio_url, local_audio_path)

meeting_agent: Agent = Agent(
    model=Gemini(id="gemini-2.0-flash"),
    tools=[OpenAITools(), ReasoningTools()],
    description=dedent("""\
        您是一个高效的会议助理 AI。
        您的目的是处理会议的音频录音，提取关键信息，
        创建可视化表示，并提供音频摘要。
    """),
    instructions=dedent("""\
        请精确执行以下步骤：
        1. 接收音频文件的路径。
        2. 使用 `transcribe_audio` 工具获取文本转录。
        3. 分析转录内容并编写简洁的摘要，突出关键讨论点、决策和行动项目。
        4. 仅基于步骤 3 中创建的摘要，生成重要的会议要点。这应该是摘要内容的概览，按适当顺序排列并以会议纪要的形式格式化。
        5. 使用 `generate_speech` 工具将会议纪要转换为音频摘要。
    """),
    markdown=True,
)

response = meeting_agent.run(
    f"请处理位于 '{local_audio_path}' 的会议录音"
)
if response.audio:
    base64_audio = base64.b64encode(response.audio[0].content).decode("utf-8")
    save_base64_data(base64_audio, Path("tmp/meeting_summary.mp3"))
    print(f"会议摘要已保存到: {Path('tmp/meeting_summary.mp3')}")
```

## 用法

<Steps>
  <Step title="安装依赖">
    ```bash
    pip install openai agno
    ```
  </Step>

  <Step title="运行示例">
    ```bash
    python ref/meeting_summarizer_agent.py
    ```
  </Step>
</Steps>

默认情况下，音频摘要将保存到 `tmp/meeting_summary.mp3`。