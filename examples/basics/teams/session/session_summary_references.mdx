---
title: 带上下文引用的会话摘要
---


此示例展示如何使用 `add_session_summary_to_context` 参数将会话摘要添加到团队上下文中，以保持对话连续性。

## 代码

```python cookbook/02_examples/teams/session/04_session_summary_references.py
"""
此示例展示如何在团队配置中使用 `add_session_summary_to_context` 参数
将会话摘要添加到团队上下文中。

通过运行以下命令在本地 Docker 上启动 postgres 数据库：cookbook/scripts/run_pgvector.sh
"""

from agno.agent.agent import Agent
from agno.db.postgres import PostgresDb
from agno.models.openai import OpenAIChat
from agno.team import Team

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

db = PostgresDb(db_url=db_url, session_table="sessions")

agent = Agent(
    model=OpenAIChat(id="gpt-5-mini"),
)

team = Team(
    model=OpenAIChat(id="gpt-5-mini"),
    members=[agent],
    db=db,
    enable_session_summaries=True,
)

# 这将创建一个新的会话摘要
team.print_response(
    "我叫约翰·多伊，我喜欢在周末去山里徒步。",
)

# 您可以使用会话存储中的现有会话摘要，而无需创建或更新任何新摘要。
team = Team(
    model=OpenAIChat(id="gpt-5-mini"),
    db=db,
    session_id="session_summary",
    add_session_summary_to_context=True,
    members=[agent],
)

team.print_response("我也喜欢打篮球。")

# 或者，您可以创建一个新的会话摘要，而不将会话摘要添加到上下文中。

# team = Team(
#     model=OpenAIChat(id="gpt-5-mini"),
#     db=db,
#     session_id="session_summary",
#     enable_session_summaries=True,
#     add_session_summary_to_context=False,
# )

# team.print_response("我也喜欢打篮球。")
```

## 用法

<Steps>

    <Snippet file="create-venv-step.mdx" />

    <Step title="安装所需库">
        ```bash
        pip install agno psycopg2-binary
        ```
    </Step>

    <Step title="设置环境变量">
        ```bash
        export OPENAI_API_KEY=****
        ```
    </Step>

    <Step title="启动 PostgreSQL 数据库">
        ```bash
        cookbook/run_pgvector.sh
        ```
    </Step>

    <Step title="运行代理">
        ```bash
        python cookbook/02_examples/teams/session/04_session_summary_references.py
        ```
    </Step>

</Steps>