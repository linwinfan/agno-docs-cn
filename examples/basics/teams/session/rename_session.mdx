---
title: Session Name Management
---


此示例演示 how to set custom 会话 names or automatic所有y generate meaningful names for better 会话 organization and identification.

## 代码

```python cookbook/02_examples/teams/session/06_rename_session.py
from agno.agent.agent import Agent
from agno.db.postgres import PostgresDb
from agno.models.openai import OpenAIChat
from agno.team import Team

db_url = "postgresql+psycopg://ai:ai@localhost:5532/ai"

db = PostgresDb(db_url=db_url)

agent = Agent(
    model=OpenAIChat(id="gpt-5-mini"),
)

team = Team(
    model=OpenAIChat(id="gpt-5-mini"),
    members=[agent],
    db=db,
)

team.print_response("Tell me a new interesting fact about space")
team.set_session_name(session_name="Interesting Space Facts")
print(team.get_session_name())

# Autogenerate session name
team.set_session_name(autogenerate=True)
print(team.get_session_name())
```

## 用法

<Steps>

    <Snippet file="create-venv-step.mdx" />

    <Step title="Install required libraries">
        ```bash
        pip install agno psycopg2-binary
        ```
    </Step>

    <Step title="Set environment variables">
        ```bash
        export OPENAI_API_KEY=****
        ```
    </Step>

    <Step title="Start PostgreSQL database">
        ```bash
        cookbook/run_pgvector.sh
        ```
    </Step>

    <Step title="Run the agent">
        ```bash
        python cookbook/02_examples/teams/session/06_rename_session.py
        ```
    </Step>

</Steps>