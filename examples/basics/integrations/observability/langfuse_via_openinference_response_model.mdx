---
title: 通过 Openinference 使用 Langfuse（带结构化输出）
---


## 代码

```python cookbook/13_integrations/observability/langfuse_via_openinference_response_model.py
import base64
import os
from enum import Enum

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.duckduckgo import DuckDuckGoTools
from openinference.instrumentation.agno import AgnoInstrumentor
from opentelemetry import trace as trace_api
from opentelemetry.exporter.otlp.proto.http.trace_exporter import OTLPSpanExporter
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import SimpleSpanProcessor
from pydantic import BaseModel, Field

LANGFUSE_AUTH = base64.b64encode(
    f"{os.getenv('LANGFUSE_PUBLIC_KEY')}:{os.getenv('LANGFUSE_SECRET_KEY')}".encode()
).decode()
os.environ["OTEL_EXPORTER_OTLP_ENDPOINT"] = (
    "https://us.cloud.langfuse.com/api/public/otel"  # 🇺🇸 美国数据区域
)
# os.environ["OTEL_EXPORTER_OTLP_ENDPOINT"]="https://cloud.langfuse.com/api/public/otel" # 🇪🇺 欧洲数据区域
# os.environ["OTEL_EXPORTER_OTLP_ENDPOINT"]="http://localhost:3000/api/public/otel" # 🏠 本地部署 (>= v3.22.0)

os.environ["OTEL_EXPORTER_OTLP_HEADERS"] = f"Authorization=Basic {LANGFUSE_AUTH}"


tracer_provider = TracerProvider()
tracer_provider.add_span_processor(SimpleSpanProcessor(OTLPSpanExporter()))
trace_api.set_tracer_provider(tracer_provider=tracer_provider)

# 开始检测 agno
AgnoInstrumentor().instrument()


class ContentType(Enum):
    NEWS = "news"
    ARTICLE = "article"
    BLOG = "blog"
    RESEARCH = "research"
    OTHER = "other"


class WebSearchResult(BaseModel):
    title: str = Field(description="搜索结果的标题")
    url: str = Field(description="搜索结果的 URL")
    snippet: str = Field(description="结果的简要描述或片段")
    content_type: ContentType = Field(description="找到的内容类型")
    relevance_score: int = Field(description="相关性评分，范围 1-10", ge=1, le=10)


agent = Agent(
    name="网络研究智能体",
    model=OpenAIChat(id="gpt-5-mini"),
    tools=[DuckDuckGoTools()],
    instructions="您是一个网络研究智能体。使用 DuckDuckGo 搜索网络并查找相关信息。分析搜索结果并提供关于您发现内容的结构化信息。",
    debug_mode=True,
    output_schema=WebSearchResult,
)

agent.print_response("What are the latest developments in artificial intelligence?")

```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 API 密钥">
  自托管或在 https://us.cloud.langfuse.com 注册账户
    ```bash
    export LANGFUSE_PUBLIC_KEY=<your-key>
    export LANGFUSE_SECRET_KEY=<your-key>
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U agno ddgs langfuse opentelemetry-sdk opentelemetry-exporter-otlp openinference-instrumentation-agno
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python cookbook/13_integrations/observability/langfuse_via_openinference_response_model.py
    ```

    ```bash Windows
    python cookbook/13_integrations/observability/langfuse_via_openinference_response_model.py
    ```
    </CodeGroup>
  </Step>
</Steps>