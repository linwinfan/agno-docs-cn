---
title: Langtrace
---


## 概述

此示例演示如何使用 Langtrace 为您的 Agno 智能体添加检测以进行跟踪和监控。

## 代码

```python
from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.yfinance import YFinanceTools
from langtrace_python_sdk import langtrace
from langtrace_python_sdk.utils.with_root_span import with_langtrace_root_span

# 初始化 Langtrace
langtrace.init()

# 创建并配置智能体
agent = Agent(
    name="股票价格智能体",
    model=OpenAIChat(id="gpt-5-mini"),
    tools=[YFinanceTools()],
    instructions="您是一个股票价格智能体。以股票分析师的风格回答问题。",
    debug_mode=True,
)

# 使用智能体
agent.print_response("What is the current price of Tesla?")
```

## 用法

<Steps>
  <Step title="安装依赖项">
    ```bash
    pip install agno openai langtrace-python-sdk
    ```
  </Step>

  <Step title="设置环境变量">
    ```bash
    export LANGTRACE_API_KEY=<your-key>
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python cookbook/observability/langtrace_op.py
    ```

    ```bash Windows
    python cookbook/observability/langtrace_op.py
    ```
    </CodeGroup>
  </Step>
</Steps>

## 注意事项

- **初始化**: 在使用智能体之前，调用 `langtrace.init()` 来初始化 Langtrace。