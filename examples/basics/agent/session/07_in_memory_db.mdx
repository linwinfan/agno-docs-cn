---
title: 内存数据库存储
---


此示例演示如何使用内存数据库进行会话存储，在不需要持久化数据库设置的情况下启用对话历史和上下文管理。

## 代码

```python 07_in_memory_db.py
"""此示例展示如何使用内存数据库。

使用此功能，您无需设置数据库即可存储会话、用户记忆等。
请记住，在生产环境中建议使用数据库。
"""

from agno.agent import Agent
from agno.db.in_memory import InMemoryDb
from agno.models.openai import OpenAIChat
from rich.pretty import pprint

# 设置内存数据库
db = InMemoryDb()

agent = Agent(
    model=OpenAIChat(id="gpt-5-mini"),
    # 使用内存数据库。所有数据库功能都可用。
    db=db,
    # 设置 add_history_to_context=true 将先前的聊天历史添加到发送给模型的上下文中。
    add_history_to_context=True,
    # 要添加到消息中的历史响应数量。
    num_history_runs=3,
    description="You are a helpful assistant that always responds in a polite, upbeat and positive manner.",
)

# -*- 创建一个运行
agent.print_response("Share a 2 sentence horror story", stream=True)
# -*- 打印内存中的消息
pprint(
    [
        m.model_dump(include={"role", "content"})
        for m in agent.get_session_messages()
    ]
)

# -*- 提出一个延续对话的后续问题
agent.print_response("What was my first message?", stream=True)
# -*- 打印内存中的消息
pprint(
    [
        m.model_dump(include={"role", "content"})
        for m in agent.get_session_messages()
    ]
)
```

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="安装库">
    ```bash
    pip install -U agno openai rich
    ```
  </Step>

  <Step title="导出您的 OpenAI API 密钥">

    <CodeGroup>

    ```bash Mac/Linux
      export OPENAI_API_KEY="your_openai_api_key_here"
    ```

    ```bash Windows
      $Env:OPENAI_API_KEY="your_openai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="创建 Python 文件">
    创建一个 Python 文件并添加上面的代码。
    ```bash
    touch 07_in_memory_db.py
    ```
  </Step>

  <Step title="运行代理">
    <CodeGroup>
    ```bash Mac
    python 07_in_memory_db.py
    ```

    ```bash Windows
    python 07_in_memory_db.py
    ```
    </CodeGroup>
  </Step>

  <Step title="查找所有示例">
  在 Agno 仓库中探索所有可用的示例。点击下面的链接在 GitHub 上查看代码：

  <Link href="https://github.com/agno-agi/agno/tree/main/cookbook/03_agents/session" target="_blank">
    GitHub 上的 Agno 示例
  </Link>
</Step>
</Steps>