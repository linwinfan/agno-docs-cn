---
title: 推理团队
mode: wide
---


构建一个推理团队，通过结合网络搜索、财务数据和透明的分析思维来处理复杂查询。此示例演示了推理工具如何使团队的决策过程可见。

## 您将学习的内容

通过构建此团队，您将理解：
- 如何集成推理工具以实现透明的分析思维
- 如何结合具有不同数据源的多个专门代理
- 如何使用Exa进行特定领域的搜索以获取财务信息
- 如何流式传输推理事件以实时查看团队的思考过程

## 使用案例

构建财务搜索平台、投资分析工具、市场情报系统或决策支持应用程序。

## 工作原理

团队通过透明推理协调专门代理：

1. **分析**: 团队负责人使用推理工具规划方法
2. **搜索**: 网络代理使用DuckDuckGo查找一般信息
3. **研究**: 财务代理从可信来源检索财务数据
4. **综合**: 团队结合发现结果并展示可见的推理过程
5. **呈现**: 输出带有支持逻辑的结构化数据

推理工具提供了团队如何思考复杂查询的透明度。

## 代码

```python reasoning_team.py
from agno.agent import Agent
from agno.models.anthropic import Claude
from agno.models.openai import OpenAIChat
from agno.team import Team
from agno.tools.duckduckgo import DuckDuckGoTools
from agno.tools.reasoning import ReasoningTools
from agno.tools.exa import ExaTools

web_agent = Agent(
    name="Web Search Agent",
    role="Handle web search requests",
    model=OpenAIChat(id="gpt-4o"),
    tools=[DuckDuckGoTools()],
    instructions=["Always include sources"],
)

finance_agent = Agent(
    name="Finance Agent",
    role="Handle financial data requests",
    model=OpenAIChat(id="gpt-4o"),
    tools=[
        ExaTools(
            include_domains=["cnbc.com", "reuters.com", "bloomberg.com", "wsj.com"],
            show_results=True,
            text=False,
            highlights=False,
        )
    ],
    instructions=["Use tables to display data"],
)

team_leader = Team(
    name="Reasoning Team Leader",
    model=Claude(id="claude-3-7-sonnet-latest"),
    members=[
        web_agent,
        finance_agent,
    ],
    tools=[ReasoningTools(add_instructions=True)],
    markdown=True,
    show_members_responses=True,
)

team_leader.print_response(
    "Tell me 1 company in New York, 1 in San Francisco and 1 in Chicago and the stock price of each",
    stream=True,
    show_full_reasoning=True,
)
```

## 期望结果

团队将通过协调网络搜索和财务数据代理来研究公司和股票价格。您将查看推理过程，了解团队如何规划方法、委派任务并综合信息。

输出包括通过推理工具展示的可见思维，显示团队如何决定使用哪个代理以及如何结合他们的发现。财务数据以表格形式呈现，并附有来自可信财经新闻媒体的来源。

## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的API密钥">
    ```bash
    export OPENAI_API_KEY=xxx
    export ANTHROPIC_API_KEY=xxx
    export EXA_API_KEY=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U agno openai anthropic ddgs exa-py
    ```
  </Step>

  <Step title="运行团队">
    <CodeGroup>
    ```bash Mac
    python reasoning_team.py
    ```

    ```bash Windows
    python reasoning_team.py
    ```
    </CodeGroup>
  </Step>
</Steps>

## 下一步

- 修改查询以研究不同类型的公司或市场
- 调整ExaTools中的`include_domains`以专注于特定的财务来源
- 切换`show_full_reasoning`以控制思考过程的可见性
- 探索 [推理工具](/basics/tools/reasoning_tools/reasoning-tools) 以获得高级分析能力