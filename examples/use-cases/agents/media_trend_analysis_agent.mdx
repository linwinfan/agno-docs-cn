---
title: 媒体趋势分析智能体
---

媒体趋势分析智能体示例展示了一个复杂的 AI 驱动工具，旨在分析媒体趋势、跟踪数字对话，并在各种在线平台上提供可操作的洞察。该智能体结合了网络搜索功能和内容抓取功能，以提供全面的趋势分析报告。

### 功能说明

该智能体专门用于：
- **趋势识别**：检测媒体报道中的新兴模式和转变
- **来源分析**：识别关键影响者和权威来源
- **数据提取**：从新闻网站、社交平台和数字媒体收集信息
- **洞察生成**：基于趋势分析提供可操作的建议
- **未来预测**：基于当前模式预测潜在发展

### 主要功能

- **多源分析**：结合 Exa 搜索工具和 Firecrawl 抓取功能
- **智能过滤**：使用基于关键词的搜索和日期过滤来获得相关结果
- **智能抓取**：仅在搜索结果不足时抓取内容
- **结构化报告**：生成带有执行摘要的综合 markdown 报告
- **实时数据**：使用可配置的时间窗口分析当前趋势

## 代码

```python cookbook/02_examples/agents/media_trend_analysis_agent.py
"""请使用以下命令安装依赖：
pip install openai exa-py agno firecrawl
"""

from datetime import datetime, timedelta
from textwrap import dedent

from agno.agent import Agent
from agno.models.openai import OpenAIChat
from agno.tools.exa import ExaTools
from agno.tools.firecrawl import FirecrawlTools


def calculate_start_date(days: int) -> str:
    """Calculate start date based on number of days."""
    start_date = datetime.now() - timedelta(days=days)
    return start_date.strftime("%Y-%m-%d")


agent = Agent(
    model=OpenAIChat(id="gpt-5-mini"),
    tools=[
        ExaTools(start_published_date=calculate_start_date(30), type="keyword"),
        FirecrawlTools(scrape=True),
    ],
    description=dedent("""\
        您是一位专业的媒体趋势分析师，专注于：
        1. 识别新闻和数字平台上的新兴趋势
        2. 识别媒体报道中的模式变化
        3. 基于数据提供可操作的洞察
        4. 预测潜在的未来发展
    """),
    instructions=[
        "根据用户指定的条件分析提供的主题：",
        "1. 使用关键词进行定向搜索",
        "2. 识别关键影响者和权威来源",
        "3. 提取主要主题和重复出现的模式",
        "4. 提供可操作的建议",
        "5. 如果获得的来源少于 2 个，才使用 firecrawl 工具抓取它们，不要抓取并使用它们生成报告",
        "6. 增长率应以百分比表示，如果无法计算则不提供增长率",
    ],
    expected_output=dedent("""\
    # 媒体趋势分析报告

    ## 执行摘要
    {发现和关键指标的高级概述}

    ## 趋势分析
    ### 量级指标
    - 讨论高峰期：{dates}
    - 增长率：{percentage or dont show this}

    ## 来源分析
    ### 主要来源
    1. {Source 1}

    2. {Source 2}


    ## 可操作洞察
    1. {Insight 1}
       - 证据：{data points}
       - 建议行动：{action}

    ## 未来预测
    1. {Prediction 1}
       - 支持证据：{evidence}

    ## 参考文献
    {带链接的详细来源列表}
    """),
    markdown=True,
    add_datetime_to_context=True,
)

# 使用示例：
analysis_prompt = """\
分析以下内容的媒体趋势：
关键词：AI 智能体
来源：verge.com, linkedin.com, x.com
"""

agent.print_response(analysis_prompt, stream=True)

# 替代提示示例
crypto_prompt = """\
分析以下内容的媒体趋势：
关键词：加密货币、比特币、以太坊
来源：coindesk.com, cointelegraph.com
"""

# agent.print_response(crypto_prompt, stream=True)

```
## 用法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 API 密钥">
    ```bash
    export OPENAI_API_KEY=xxx
    export EXA_API_KEY=xxx
    export FIRECRAWL_API_KEY=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U agno openai exa-py agno firecrawl
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python cookbook/02_examples/agents/media_trend_analysis_agent.py
    ```

    ```bash Windows
    python cookbook/02_examples/agents/media_trend_analysis_agent.py
    ```
    </CodeGroup>
  </Step>
</Steps>

## 自定义选项

您可以通过以下方式自定义智能体的行为：
- **调整时间窗口**：修改 `calculate_start_date()` 函数以分析不同的时间段
- **添加来源**：在分析提示中包含额外的网站或平台
- **修改关键词**：更改搜索词以专注于特定主题或行业
- **自定义输出**：修改 `expected_output` 模板以匹配您的报告需求

## 示例提示

以下是您可以尝试的一些额外提示示例：

```python
# 技术趋势
tech_prompt = """\
分析以下内容的媒体趋势：
关键词：人工智能、机器学习、自动化
来源：techcrunch.com, arstechnica.com, wired.com
"""

# 商业趋势
business_prompt = """\
分析以下内容的媒体趋势：
关键词：远程工作、数字化转型、可持续发展
来源：forbes.com, bloomberg.com, hbr.org
"""

# 娱乐趋势
entertainment_prompt = """\
分析以下内容的媒体趋势：
关键词：流媒体、游戏、社交媒体
来源：variety.com, polygon.com, theverge.com
"""
```
