---
title: 会议总结智能体
---


此智能体使用 OpenAITools（transcribe_audio、generate_image、generate_speech）
来处理会议录音、总结、可视化并创建音频摘要。

## 代码

```python cookbook/02_examples/agents/meeting_summarizer_agent.py
"""示例：会议总结与可视化智能体

此脚本使用 OpenAITools（transcribe_audio、generate_image、generate_speech）
来处理会议录音、总结、可视化并创建音频摘要。

需要：pip install openai agno
"""

import base64
from pathlib import Path
from textwrap import dedent

from agno.agent import Agent
from agno.models.google import Gemini
from agno.tools.openai import OpenAITools
from agno.tools.reasoning import ReasoningTools
from agno.utils.media import download_file, save_base64_data

input_audio_url: str = (
    "https://agno-public.s3.us-east-1.amazonaws.com/demo_data/sample_audio.mp3"
)

local_audio_path = Path("tmp/meeting_recording.mp3")
print(f"Downloading file to local path: {local_audio_path}")
download_file(input_audio_url, local_audio_path)

meeting_agent: Agent = Agent(
    model=Gemini(id="gemini-2.0-flash"),
    tools=[OpenAITools(), ReasoningTools()],
    description=dedent("""\
        您是一个高效的会议助理 AI。
        您的目的是处理会议录音，提取关键信息，
        创建可视化表示，并提供音频摘要。
    """),
    instructions=dedent("""\
        请精确遵循以下步骤：
        1. 接收音频文件的路径。
        2. 使用 `transcribe_audio` 工具获取文本转录。
        3. 分析转录内容并撰写简洁的摘要，突出关键讨论点、决策和行动项。
        4. 仅基于第 3 步创建的摘要，生成重要的会议要点。这应该是摘要内容的概览，按照正确的顺序和格式整理成会议纪要的形式。
        5. 使用 `generate_speech` 工具将会议纪要转换为音频摘要。
    """),
    markdown=True,
)

response = meeting_agent.run(
    f"Please process the meeting recording located at '{local_audio_path}'"
)
if response.audio:
    base64_audio = base64.b64encode(response.audio[0].content).decode("utf-8")
    save_base64_data(base64_audio, Path("tmp/meeting_summary.mp3"))
    print(f"Meeting summary saved to: {Path('tmp/meeting_summary.mp3')}")
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 API 密钥">
    ```bash
    export OPENAI_API_KEY=xxx
    export GOOGLE_API_KEY=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U agno openai google-genai
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python cookbook/02_examples/agents/meeting_summarizer_agent.py
    ```

    ```bash Windows
    python cookbook/02_examples/agents/meeting_summarizer_agent.py
    ```
    </CodeGroup>
  </Step>
</Steps>
